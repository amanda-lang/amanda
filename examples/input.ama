# Testar funções de input


#Testar se um número é par
func e_par(num : int): bool
    se num % 2 == 0 entao
        retorna verdadeiro
    senao
        retorna falso
    fim
fim

num : int = leia_int("Digite um número: ")
mostra e_par(num)

#Testar o leia
msg : texto = leia("Digite a mensagem que deseja imprimir: ")
mostra msg

user_pi : real = leia_real("Digite o valor de pi: ")

se user_pi == 3.14 entao
    mostra "Certo"
senao
    mostra "Errado"
fim
