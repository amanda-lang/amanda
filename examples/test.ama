# Comments:
# This is a new and improved comment in amanda

#Variable declarations:

# Since amanda is statically typed, every type must be explicitly
# Declared

var ci : real;

var a : int = 50 + 2  # instructions can be split by newline or semicolon

var name : real = --20 + 2 -+5

var pi : real = 3.14  

var logico : bool = verdadeiro
var yu : int
# Function declarations:
# No more syntactic noise



mostra name



func add(a : int, b: int) : int
    yu
    retorna a + b
fim 		#Maybe fim func ??

#enquanto and para
enquanto pi > 0 faca
    pi -= 1;
fim



mostra add(a,a)


se pi < 4 entao
    mostra 10
fim


se pi < 4 entao
    mostra 10
senao
   mostra 11
fim




mostra pi

#class declaration

classe Objecto
    
    var uuid : int
    var hash : int
    
    func em_int():int
	retorna 1
    fim

    func mostra_hash():int
    	retorna hash
    fim
	
fim
