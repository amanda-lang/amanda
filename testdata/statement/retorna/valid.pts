$ testing function returns




$INT
var int i;
def soma(int a,int b):int{
  retorna a+b;
}


i = soma(2,2);
mostra i; $ expect 4

$ TEXTO
var texto str = "meu";
def double(texto str): texto{
  str += str;
  retorna str;
}
str = double(str);
mostra str; $ expect "meumeu"

$Bool
var bool logico;

def nega_bool(bool logico):bool{
  retorna !logico;
}

logico = nega_bool(logico);
mostra logico; $ expect verdadeiro


$ REAL
var real pi = 3.14;

def flip_sign(real n): real{
  retorna -n;
}

mostra flip_sign(pi); $ expect -3.14



$Nested return
def func():int{
  {
    retorna 0;
  }
}

mostra func(); $ expect 0
