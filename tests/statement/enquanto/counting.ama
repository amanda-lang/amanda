#Test of enquanto loop

var i:int

enquanto i != 5 faca
  i += 1;
fim

mostra i; # expect 5

# with block
i = 0;

enquanto (i != 10) faca
  i += 1;
fim

mostra i; # expect 10


var logico:bool


enquanto nao logico faca
    se i == 20 entao
        logico = verdadeiro
    senao
        i += 1
    fim
fim
