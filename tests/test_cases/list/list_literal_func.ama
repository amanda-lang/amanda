list : [] int = [int:]
list = [int: 1, 2, 3, 4]


func duplica(list : []int): []int
    new_list : []int = lista(int, tamanho(list))
    para i de 0..tamanho(list) faca
        new_list[i] = list[i] * 2
    fim

    retorna new_list
fim

mostra duplica(list)

#[output]:Erro: Funções locais não têm acesso ao escopo envolvente.
