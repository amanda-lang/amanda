
registo Mensagem
    sender: texto
    to: texto
    corpo: texto
fim


met Mensagem::enviar(alvo)
    escrevaln(alvo.sender)
fim

met Mensagem::enviar(alvo)
    escrevaln(alvo.sender)
fim

msg.enviar()
#[output]: Erro: O método 'enviar' do tipo 'Mensagem' já foi declarado anteriormente.
