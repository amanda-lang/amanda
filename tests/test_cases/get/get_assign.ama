

#uma classe pra contar loool
classe Acumulador
    
    #Fields must be declared explicitly
     contador : int 
    #Can have methods
    func incrementa(diff : int)
        contador += diff
    fim

    func get_contador():int
        retorna contador
    fim

fim



acum : Acumulador = Acumulador(4)
num : int = acum.get_contador #expect error

#[output]:Erro: o identificador 'get_contador' não é uma referência válida.