
# Class with constructor that has return type

classe A
    
    #Fields must be declared explicitly
     field : int 

    func constructor(field : int): int
        eu.field = field
        retorna field
    fim

fim


a : A = A(5) # expect error
