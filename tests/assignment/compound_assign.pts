$ Test compound assignments


var int a = 1;

a += 1;
mostra a; $ expect 2

a -= 1;
mostra a; $ expect 1


var int b = 2;

a *= b;
mostra a;  $ expect 2


a /= b;
mostra a; $ expect 1

var texto text = "eu";
text += text;
mostra text; $ expect eueu
