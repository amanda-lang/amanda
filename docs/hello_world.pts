$* this is
a multiline comment *$
$* these are the different types:
 int,real,bool,texto *$
$ The two bool types
decl texto l = "verdadeiro";
decl texto a = "falso";


"teste" + "teste";
$ decl int a;
l=a="teste";
$ A string
decl texto nome = "Stenio";
a=l;
$ This is a variable declaration
declara int antonio;
$ or short form
decl int b;

$ this is an array
vector int vet[3];

$ Access element
b recebe vet[2];

b recebe vet[3+4];
$ this is an assignment
a recebe '10';

b = b+b;
b = --2-5;
$ or short form
b = 90 + 10;

$ Expressions
$ b recebe (2+a)*b; $ Don't forget the semi!!!!!
$ b = b*2;



$ function declarations
defina soma(int a,int b): int{

  decl int ab;
  decl real angola;

  retorna ab;
}

$ or for 'procedure'
defina divisao(texto a,int b){

  vector int tem[3];
  mostra 3/b;
}


$ or short form
def divisao2(int a,int b): int{
  vector int tem[3];
  mostra a/b;
}



$ Function call
mostra soma(1+b,2+2);
divisao("si",b);
