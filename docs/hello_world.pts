$* this is
a multiline comment *$

$* these are the different types:
 int,real,bool,string *$

$ The two bool types
decl bool l = "verdadeiro";
decl bool a = "falso";

$ A string
decl string nome = "Stenio";

$ This is a variable declaration
declara int a;

$ or short form
decl int b;

$ this is an array
vector int vet[3];

$ Access element
b recebe vet[2];

$ this is an assignment
a recebe 10;



$ or short form
b = 90 + a;

$ Expressions
b recebe (2+a)*b; $ Don't forget the semi!!!!!
b = b*2;
c = --2--5;
c = divisao(a,b);
d = c = a = assign(a);



$ function declarations
defina soma(int a,int b):int {

  retorna a+b;
}

$ or for 'procedure'
defina divisao(int a,int b){

  mostra a/b;
}

$ or short form
def divisao(int a,int b){

  mostra a/b;
}



$ Function call

mostra soma(a+b,b);
divisao(a,b);
